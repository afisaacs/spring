spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dev
    username: root
    password: giitic
  jpa:
    hibernate:
      ddl-auto: create-drop
    generate-ddl: true      
    show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true